/**
 * Generate a standalone HTML fragment for a given month, for CMS paste-in.
 *
 * Post-launch. Will port the significant existing work on this deliverable
 * (to be supplied separately) rather than rebuilding from scratch.
 */
console.error('build-monthly: not yet implemented — prior art to be ported post-launch.');
process.exit(0);
